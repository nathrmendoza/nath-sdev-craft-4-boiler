{# make sure component filename similar to block handle #}

{% if entry is not null %}
    {% if entry.components is not null %}
        {% for block in entry.components %}
            {% include ['components/' ~ block.type ~ '.twig', 'components/fallback'] ignore missing with {block: block} %}
        {% endfor %}
    {% endif %}
{% endif %}